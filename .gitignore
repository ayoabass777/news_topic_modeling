# ── Python build artifacts ─────────────────────────────────────────
__pycache__/
*.py[cod]
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# ── Environment & secrets ──────────────────────────────────────────
.env
.env.*
!.env.example
*.csv

# ── Data / runtime artifacts ───────────────────────────────────────
data/
logs/
*.log
*.db
*.sqlite
*.jsonl
*.parquet

# ── OS noise ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE ───────────────────────────────────────────────────────────
.vscode/
.idea/

# ── OLD flat package copies (superseded by news_topic_model/) ─────
# These directories existed before the proper package structure.
# They are intentionally untracked. Do not re-add them.
# Leading slash anchors to repo root only (won't affect news_topic_model/pipeline/ etc.)
/api_adapter/
/pipeline/
/storage/
/utils/

# ── Keep the proper package and all other source files ────────────
# (All other files not matched above are tracked by default)
