# Example environment configuration for news_topic_model
# Copy this file to `.env` and populate with your secrets before running the pipeline.

# NewsAPI / RapidAPI credentials
NEWSAPI_URL=https://example-rapidapi-endpoint/v1/search
NEWSAPI_KEY=your-rapidapi-key-here
NEWSAPI_HOST=your-rapidapi-host-here

# Optional: customise logging output directory
LOG_DIR=./logs

# Optional: tweak target totals (fallbacks are defined in code)
TARGET_TOTAL=100
